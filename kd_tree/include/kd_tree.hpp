#include <iostream>
#include <vector>
#include <string>
#include <map>
#include <cmath>

// Class for storing point cloud data.
class PointCloud{

    // Constructor.
    PointCloud(){
        : point_cloud = {}
    }

    // Member variables.
    std::vector<std::vector<double > > point_cloud;

    // Member functions.
    
}